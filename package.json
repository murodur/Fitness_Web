{
  "name": "fitness-tracker",
  "version": "1.0.0",
  "description": "Fitness Goal Tracker Web Application",
  "main": "app.js",
  "scripts": {
    "start": "node app.js",
    "dev": "nodemon app.js",
    "build": "mkdir -p dist && cp -r public/* dist/ && cp -r views dist/"
  },
  "dependencies": {
    "dotenv": "^16.3.1",
    "express": "^4.18.2",
    "express-validator": "^7.0.1",
    "method-override": "^3.0.0",
    "mongoose": "^7.6.3",
    "pug": "^3.0.2"
  },
  "devDependencies": {
    "nodemon": "^3.0.1"
  }
}
